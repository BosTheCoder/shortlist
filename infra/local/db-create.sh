#!/usr/bin/env bash
# MANAGED BY demo-tools — DO NOT EDIT. Run `just sync` to update.
set -euo pipefail
echo "local uses file-based SQLite at ./data — nothing to provision."
echo "The database file is created by the app on first write and persists on the host."
